---
import CreateTransaction from './CreateTransaction.svelte';
import { getBankAccounts } from '../src/util/api/bankAccount';

const userSession = Astro.cookies.get('session')!;
const data = await getBankAccounts(userSession.value);
---

<CreateTransaction client:load {data} />
